Algoritmo "testeEnquanto"

Var

   x, soma : inteiro


Inicio

      escreva("digite o primeiro numero:  ")
      leia(x)
      
      soma <- 0
      enquanto x <> 0 faca
         soma <- soma + x
         escreva("digite outro numero: ")
         leia(x)
      fimenquanto

      escreval("soma = ", soma)
Fimalgoritmo